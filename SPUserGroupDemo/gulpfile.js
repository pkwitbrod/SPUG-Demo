'use strict';
//This is where you would add other GULP tasks besides what Microsoft Provides. 
const gulp = require('gulp');
const build = require('@microsoft/sp-build-web');

build.initialize(gulp);
